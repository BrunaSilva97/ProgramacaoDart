import 'package:flutter/material.dart';

void main(){
  
  String nome = "Bruna";
  runApp(
    MaterialApp(
      home: Scaffold(
        backgroundColor: Colors.blue,
        body: Center(
          child: Row(
            children: [
              Text(nome,
            style: TextStyle(
              fontSize:100,
              color: Colors.orange,
              fontWeight: FontWeight.bold)),
              
              Text("Aluno 2",style: TextStyle(
              fontSize:100,
              color: Colors.green,
              fontWeight: FontWeight.bold)),
              
              Text("Aluno 3",style: TextStyle(
              fontSize:100,
              color: Colors.red,
              fontWeight: FontWeight.bold)),
              
            ]
          )
          
        ),
      ),
    ),
  );
}

import 'package:flutter/material.dart';

void main(){
  

  runApp(MeuApp());
}

class MeuApp extends StatelessWidget{
  const MeuApp({super.key});
  
  @override
  Widget build(BuildContext context){
    const double nota = 8;
    const String nome = 'Amanda';
    final String resultado;
    
    if(nota >= 7){
      resultado = 'Aprovado';
    }else{
        resultado = 'Reprovado';
    }
     
    return MaterialApp(
      debugShowCheckedModeBanner: false,
    home: Scaffold(
      backgroundColor: Colors.black,
    body:Center(
    child: Text('$nome: obteve nota $nota e foi $resultado',
               style: TextStyle(
               fontSize: 40,
               fontWeight: FontWeight.bold,
               color: nota >= 7 ? Colors.green : Colors.red))
    )
    )
    );
    
  }
}

